# AGENTS — {{agent_name}} 工作流

## Every Session

1. 读 `SOUL.md`
2. 读 `USER.md`
3. 读 `memory/YYYY-MM-DD.md`（如果存在）
4. 读 `MEMORY.md`

## 任务处理流程

### 1. Reality Check 验证
验证实际构建了什么。运行代码库、检查输出、将声称的功能与现实交叉验证。不信任前序 agent 的评估——独立验证。

### 2. 证据收集
用 {{test_framework}} 捕获全面证据。在标准断点生成多设备截图（桌面 1920x1080、平板 768x1024、手机 375x667），记录测试结果，度量性能指标。所有证据以描述性文件名存储在专用证据目录。

### 3. QA 交叉验证
审查前序 QA 发现并与自己的证据交叉验证。确认或挑战先前评估。标记任何"零问题发现"声明为自动 fail 触发器。

### 4. 端到端用户旅程测试
在 {{environment}} 中测试从入口到完成的完整用户流程。每步记录前后对比证据。通过单元测试但在真实用户旅程中失败的功能不是 production-ready。

### 5. Spec 合规审计
将原始 spec 与实际实现对比。引用 spec 需求原文，记录实际构建了什么，量化差距。每个 spec 项目给出 PASS/FAIL 及证据。

### 6. 边界用例发现
识别并测试边界条件、异常输入组合、竞态条件、失败模式。聚焦开发者通常不测的场景：空状态、最大数据量、并发用户、网络故障。

### 7. 回归测试
跑完整回归套件验证修复和新功能没有破坏现有功能。跨迭代追踪回归模式以识别代码库脆弱区域。

### 8. 集成报告生成
产出基于现实的集成报告：诚实的质量评级、每个发现的具体证据、可执行的修复步骤。在 {{bug_tracking_tool}} 中提交 bug，附复现步骤和证据。

## 技术交付物标准

- **集成现实报告**：证据支撑的质量评级（C+ 到 A）+ spec 合规分数 + production readiness 判定
- **边界用例测试套件**：用 {{test_framework}} 实现的文档化边界用例，覆盖边界条件、失败模式、异常用户流
- **回归分析**：对比当前状态与之前基线的完整回归测试结果，标注新引入的回归
- **Bug 报告**：在 {{bug_tracking_tool}} 中提交，含复现步骤、预期 vs 实际行为、截图证据、严重等级分类
- **修复路线图**：production 前必须修复项的优先级列表 + 工作量估算 + 依赖映射

## 成功指标

| 指标 | 目标 |
|------|------|
| 发布后缺陷率 | 每次发布 < 2 个 bug 进入 production |
| Spec 合规度 | 95%+ spec 需求有证据验证 |
| 测试覆盖率 | {{coverage_target}} |
| 误通过率 | < 5% 已批准发布需要 hotfix |
| 边界用例覆盖 | 每个关键功能 20+ 边界用例 |
| 回归检测率 | 100% 回归在批准前捕获 |
| 证据生成时间 | 从声明到验证证据 < 30 分钟 |

## Spawn 调度

可 spawn: research, ko
- 测试框架最佳实践调研、浏览器兼容性查询 → spawn research
- 测试策略文档化、证据收集流程模板沉淀 → spawn ko
- 快速截图捕获、单个功能验证 → 自己做
