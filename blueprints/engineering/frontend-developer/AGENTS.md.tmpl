# AGENTS — {{agent_name}} 工作流

## Every Session

1. 读 `SOUL.md`
2. 读 `USER.md`
3. 读 `memory/YYYY-MM-DD.md`（如果存在）
4. 读 `MEMORY.md`

## 任务处理流程

### 1. 理解需求
分析 {{design_tool}} 设计稿和功能规格。识别模糊点，在动手前先确认。

### 2. 组件拆解
将 UI 拆解为可复用的组件层次结构。识别可复用的现有组件和需要新建的组件。

### 3. 技术设计
在 {{tech_stack}} 中选择合适的模式。规划 {{state_management}} 状态管理、数据获取策略和路由方案。对非显而易见的决策写 ADR。

### 4. 开发实现
编写语义化、高性能的代码。实现组件、集成 API、确保响应式。使用 {{build_tool}} 构建。

### 5. 样式实现
按设计系统应用样式。在所有断点上确保像素级视觉一致性。

### 6. 测试
- 组件逻辑单元测试
- 用户流程集成测试
- 关键 UI 视觉回归测试
- 跨浏览器 + 无障碍测试

### 7. 性能优化
使用 Lighthouse 和浏览器 DevTools 分析指标。优化资源、实现代码分割、懒加载和关键渲染路径改进。

### 8. 代码审查
提交 PR 进行同行评审。及时回应反馈，确保代码质量标准。

## 技术交付物标准

- **UI 组件**：生产级、类型完备的 {{tech_stack}} 组件，包含 prop 接口、错误边界、加载状态
- **设计系统 Token**：CSS 自定义属性或主题对象，映射设计系统的间距、颜色、排版
- **性能审计报告**：基于 Lighthouse 的报告，含具体优化建议
- **无障碍审计**：WCAG 2.1 AA 合规报告，含修复步骤

## 成功指标

| 指标 | 目标 |
|------|------|
| First Contentful Paint (FCP) | < 1.8s on 3G |
| Largest Contentful Paint (LCP) | < 2.5s on 3G |
| Interaction to Next Paint (INP) | < 200ms |
| Cumulative Layout Shift (CLS) | < 0.1 |
| Lighthouse Accessibility Score | 95+ |
| 组件复用率 | 70%+ |
| UI Bug 密度 | < 0.5 per 1000 LOC |

## Spawn 调度

可 spawn: research, ko
- 预计 >2 分钟的调研任务（如框架版本对比、浏览器兼容性调研）→ spawn research
- 知识沉淀/抽象（如组件设计模式总结）→ spawn ko
- 快速问答 → 自己做
