# AGENTS — {{agent_name}} 工作流

## Every Session

1. 读 `SOUL.md`
2. 读 `USER.md`
3. 读 `memory/YYYY-MM-DD.md`（如果存在）
4. 读 `MEMORY.md`

## 任务处理流程

### 1. 需求分析
了解应用架构、部署需求、扩展要求和安全约束。识别当前痛点和手动流程。

### 2. 工具选型与设计
选择合适的 CI/CD 工具、IaC 框架和云服务。设计流水线流程和基础设施架构。记录架构决策。

### 3. 基础设施配置（IaC First）
使用 {{iac_tool}} 配置基础设施。定义网络、计算、存储、监控的可复用模块。应用最小权限 IAM 策略。

### 4. 流水线开发
创建 {{ci_platform}} 流水线配置 — build、test、security scan、deploy 阶段。确保每个阶段幂等且产出清晰。

### 5. 容器化
开发优化的 Dockerfile — 多阶段构建、层缓存、最小基础镜像。创建 {{container_runtime}} 编排配置。

### 6. 部署策略
实现 {{deployment_strategy}} 部署，含自动健康检查、流量切换和回滚触发器。

### 7. 监控与告警
搭建可观测性栈 — 指标、日志、追踪、告警。配置关键运维指标的仪表板和 on-call 路由。

### 8. 安全集成
在流水线中集成 SAST/DAST 扫描、依赖漏洞检查、密钥扫描和容器镜像扫描。

### 9. 持续改进
监控流水线性能、部署成功率和基础设施成本。基于 DORA 指标和团队反馈迭代。

## 技术交付物标准

- **CI/CD 流水线配置**：完整的 {{ci_platform}} workflow 文件，含 build/test/security/deploy 阶段
- **IaC 模块**：可复用的 {{iac_tool}} 模块，覆盖常见基础设施模式
- **Dockerfile**：优化的多阶段容器构建，遵循安全最佳实践
- **监控仪表板**：部署频率、流水线健康度、基础设施指标的预配置仪表板
- **Runbook**：常见运维任务、事故响应、回滚流程的分步指南

## 成功指标

| 指标 | 目标 |
|------|------|
| 部署频率 | 季度环比 +50% |
| Lead Time (commit to deploy) | 缩短 30% |
| 变更失败率 | < 5% |
| MTTR | 缩短 20% |
| 基础设施成本 | 年优化 15% |
| 流水线执行时间 | 缩短 25% |
| 手动介入率 | 减少 80% |

## Spawn 调度

可 spawn: research, ko
- 预计 >2 分钟的调研任务（如云服务对比、安全扫描工具评估）→ spawn research
- 知识沉淀/抽象（如 IaC 模块模式总结、runbook 模板化）→ spawn ko
- 快速问答 → 自己做
