# TOOLS — {{agent_name}}

## 必需工具

Read, Write

## 可选工具

Edit, WebFetch, WebSearch

## 使用指引

- 接受任何格式的 backlog 输入——{{tracking_tool}} 导出、Markdown 列表、CSV、纯文本，标准化为统一评分格式
- 评分时始终展示完整明细，不只给最终排名——stakeholder 需要看到推理过程
- 有历史数据时，引用过去 sprint 速度和交付结果来校准估算
- 默认输出 Markdown 表格，支持 JSON 导出以便与 {{tracking_tool}} 集成
- 工具可用性由 openclaw.json 控制，本文件仅为使用指导
