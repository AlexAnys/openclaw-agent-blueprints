# AGENTS — {{agent_name}} 工作流

## Every Session

1. 读 `SOUL.md`
2. 读 `USER.md`
3. 读 `memory/YYYY-MM-DD.md`（如果存在）
4. 读 `MEMORY.md`

## 任务处理流程

### 1. 需求分析与应用规划
- 将业务场景映射到飞书能力模块（机器人、消息卡片、审批流程、多维表格、SSO、小程序）。
- 在飞书开放平台创建应用——选择 {{feishu_app_type}}。
- 规划所需权限 scope——遵循最小权限原则列出所有 API 权限。
- 评估需要的能力：事件订阅、卡片交互、审批集成、数据同步。

### 2. 认证与基础设施搭建
- 配置应用凭证和密钥管理（环境变量或 secrets manager——绝不硬编码）。
- 实现 Token 获取与缓存，带过期处理（提前 5 分钟过期以避免边界问题）。
- 搭建 Webhook 服务，配置事件订阅 URL，完成 verification challenge。
- 部署到公网可访问环境（或用 ngrok 等工具做本地开发穿透）。

### 3. 核心功能开发

**机器人开发（Bot）**:
- 自定义 Webhook 机器人和应用机器人（支持命令、对话、卡片回调）。
- 消息类型：文本、富文本、图片、文件、交互式消息卡片。
- 群管理：机器人入群、@bot 触发、群事件监听。

**消息卡片（Message Cards）**:
- 使用 Card Builder 或原生 JSON 构建交互卡片。
- 处理回调：按钮点击、下拉选择、日期选择。
- 通过 `message_id` 更新已发送卡片内容。

**审批流程（Approval Workflow）**:
- API 创建/管理审批定义。
- 提交审批实例、查询状态、发送催办。
- 订阅审批状态变更事件驱动下游业务。

**多维表格同步（Bitable Sync）**:
- 表记录 CRUD（批量创建限 500 条/请求，批次间 200ms 延迟）。
- 自定义字段类型、视图管理、筛选排序。
- 与外部数据库/ERP 双向同步。

**单点登录（SSO）**:
- OAuth 2.0 授权码流程实现 web 应用自动登录。
- 飞书扫码登录集成第三方网站。
- 通讯录事件订阅实现用户信息和组织架构同步。

**飞书小程序（Mini Program）**:
- 小程序开发框架和组件库。
- JSAPI 调用、容器差异、发布流程。

### 4. 测试与上线
- 使用飞书开放平台 API 调试器验证每个接口。
- 测试事件回调可靠性：重复投递、乱序事件、延迟事件。
- 最小权限检查：移除开发期间多申请的权限。
- 发布应用版本，配置可用范围（全员/指定部门）。
- 搭建监控：Token 获取失败、API 调用错误、事件处理超时。

## 技术交付物标准

- **飞书应用项目**: 清晰的项目结构，分离 config/auth/bot/approval/bitable/sso/webhook/utils 模块。
- **Token 管理模块**: SDK 客户端初始化带缓存，或手动 Token Manager 带过期处理。
- **消息卡片模板**: 常见场景的交互卡片构建器（审批通知、数据报表、操作确认）。
- **事件分发器**: Webhook 服务处理事件订阅——验证、幂等处理、异步业务逻辑。
- **集成测试套件**: 覆盖 happy path、认证失败、重复事件、限流、降级场景。

## 成功指标

| 指标 | 目标 |
|------|------|
| 接口成功率 | > 99.5% |
| 事件处理延迟 | < 2 秒端到端 |
| 卡片渲染成功率 | 100%（发布前在 Card Builder 验证） |
| Token 缓存命中率 | > 95% |
| 审批效率提升 | 比手动操作提升 50%+ |
| 数据同步可靠性 | 零数据丢失，自动错误补偿 |
| 权限 scope 最小化 | 生产环境只保留严格必要的 scope |

## Spawn 调度

可 spawn: research, ko
- 调研飞书新 API、SDK 版本变更、第三方集成方案 → spawn research
- 构建消息卡片模板库、通用 Webhook 处理框架、认证模块 → spawn ko
- 单个 API 调试、小功能开发、Bug 修复 → 自己做
