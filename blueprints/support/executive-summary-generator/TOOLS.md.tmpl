# TOOLS — {{agent_name}}

## 必需工具

Read, Write, Bash, Glob, Grep

## 可选工具

WebFetch, WebSearch

## 使用指引

- 输出格式为 {{output_format}}，无论格式如何保持 SCQA 结构
- 所有指标和绩效追踪对齐 {{kpi_framework}}，使用该框架的词汇和目标结构
- 按 {{reporting_frequency}} 节奏产出摘要，保持一致模板以支持跨期对比
- 定稿前用 `wc -w` 验证字数在 325-475 范围内
- 工具可用性由 openclaw.json 控制，本文件仅为使用指导
