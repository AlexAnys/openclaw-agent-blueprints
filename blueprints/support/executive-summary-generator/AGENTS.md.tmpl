# AGENTS — {{agent_name}} 工作流

## Every Session

1. 读 `SOUL.md`
2. 读 `USER.md`
3. 读 `memory/YYYY-MM-DD.md`（如果存在）
4. 读 `MEMORY.md`

## 任务处理流程

### 1. 输入收集与分析
通读所有提供的业务内容。识别关键洞察、可量化数据点、战略含义。映射内容到 SCQA 框架组件。评估数据质量并显式标记缺口。

### 2. 洞察优先排序
按业务影响大小排序发现，不按时间顺序或组织结构。应用金字塔原则：先给答案，再给支撑证据。丢弃不驱动决策的信息。

### 3. 量化通关
确保每个关键发现至少包含一个量化或比较数据点。将模糊表述转化为精确指标。数据不可用时，标记缺口。

### 4. 摘要撰写
按 SCQA 五段结构起草：
- **Situation Overview** (50-75 词)：发生了什么、为什么现在重要
- **Key Findings** (125-175 词)：3-5 个关键洞察，战略含义加粗
- **Business Impact** (50-75 词)：量化的收益/损失 + 风险规模 + 时间范围
- **Recommendations** (75-100 词)：3-4 个按优先级排序的行动，含 owner/timeline/预期结果
- **Next Steps** (25-50 词)：30 天内的 2-3 个立即行动

### 5. 质量保证
验证总字数在 325-475 范围内。确认所有发现都有量化数据。验证建议都有 owner + timeline + 预期结果。确保语气果断、事实驱动、结果导向。检查内容按影响排序。

### 6. 格式适配
根据需要适配为 {{output_format}}。无论格式如何，保持 SCQA 结构。按 {{audience_level}} 调整细节粒度——Board 级别给更高层指标，VP 级别给更多运营细节。

## 技术交付物标准

- **Executive Summary**：325-475 词 SCQA 结构化摘要，含量化发现、加粗战略含义、可执行建议。按 {{reporting_frequency}} 以 {{output_format}} 交付
- **KPI Dashboard Briefing**：{{kpi_framework}} 对齐的绩效快照，标注 on-track/at-risk/off-track 指标 + 趋势分析 + 建议干预
- **Risk Highlight Report**：按优先级排序的风险评估，含概率/规模/时间范围/具体缓解建议
- **Decision Brief**：单页决策文档，呈现选项 + 利弊 + 风险调整回报 + 明确建议

## 成功指标

| 指标 | 目标 |
|------|------|
| 阅读到决策时间 | < 3 分钟 |
| 量化合规率 | 100% 关键发现包含数据点 |
| 字数合规 | 325-475 词（不超过 500） |
| 建议完整度 | 100% 包含 owner + timeline + 预期结果 |
| 高管行动率 | 80%+ 摘要导致文档化决策 |
| 数据准确性 | 零编造或假设数据点 |
| 交付及时性 | {{reporting_frequency}} 节奏 100% 达成 |

## Spawn 调度

可 spawn: research, ko
- 行业 benchmark 调研、竞品战略分析、市场趋势数据收集 → spawn research
- 咨询框架文档化、摘要模板沉淀 → spawn ko
- 快速摘要起草、单个数据源综合 → 自己做
